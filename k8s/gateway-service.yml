apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
      - name: gateway
        image: ghcr.io/greeta-twitter-01/gateway-service:a70d805cea056c33014758aa36f9b5f3734d11c8
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9000
        - containerPort: 8000
        env:
        - name: SERVER_PORT
          value: "9000"
        - name: BPL_JVM_THREAD_COUNT
          value: "50"
        - name: BPL_DEBUG_ENABLED
          value: "true"
        - name: BPL_DEBUG_PORT
          value: "8000"
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer:9001"
        - name: INVENTORY_SERVICE_URL
          value: "http://inventory:9002"
        - name: ORDER_SERVICE_URL
          value: "http://order:9003"
        - name: SPRING_REDIS_HOST
          value: "gateway-redis"
        - name: SPRING_KEYCLOAK_SERVER_URL
          value: "http://stock-keycloak:8080"
        - name: SPRING_KEYCLOAK_SERVER_EXTERNAL_URL
          value: "http://localhost:8080"

        lifecycle:
          preStop:
            exec:
              command: ["sh", "-c", "sleep 5"]

---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
  - name: prod
    port: 9000
    targetPort: 9000
  - name: debug
    port: 8000
    targetPort: 8000