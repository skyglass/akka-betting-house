apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory
  labels:
    app: inventory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory
  template:
    metadata:
      labels:
        app: inventory
    spec:
      containers:
      - name: inventory
        image: ghcr.io/greeta-twitter-01/inventory-service:a70d805cea056c33014758aa36f9b5f3734d11c8
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9002
        - containerPort: 8002
        env:
        - name: SERVER_PORT
          value: "9002"
        - name: BPL_JVM_THREAD_COUNT
          value: "50"
        - name: BPL_DEBUG_ENABLED
          value: "true"
        - name: BPL_DEBUG_PORT
          value: "8002"
        - name: SPRING_KEYCLOAK_SERVER_URL
          value: "http://stock-keycloak:8080"
        - name: SPRING_KEYCLOAK_SERVER_EXTERNAL_URL
          value: "http://localhost:8080"          
        lifecycle:
          preStop:
            exec:
              command: ["sh", "-c", "sleep 5"]

---
apiVersion: v1
kind: Service
metadata:
  name: inventory
  labels:
    app: inventory
spec:
  selector:
    app: inventory
  ports:
  - name: prod
    port: 9002
    targetPort: 9002
  - name: debug
    port: 8002
    targetPort: 8002